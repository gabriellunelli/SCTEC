function criarCarrinho() {
    const itens = [];
    // 1. total() somando preco * qtd
    // 2. adicionar(nome, preco, qtd)
    // 3. remover(nome)
    // 4. resumo() com o texto final
  }
   
  const aplicarCupom = (pct) => (valor) =>
    // 5. devolva o valor com desconto
  